# EchoMind 简历包装指南

本文档用于把 EchoMind 项目包装成简历、面试和项目展示中可讲清楚的经历。建议按真实实现描述，量化数据如果没有压测或标注集支撑，应写成“设计目标”或“在内置评测集上”。

包装重点：EchoMind 当前已经支持评测能力，建议在简历、面试和项目介绍中明确强调 `/eval/run`、LLM-as-Judge、意图识别 Accuracy/Macro-F1、端到端回复质量评分、回归检测和优化建议。这是区分“普通聊天 Demo”和“可持续迭代的 Agent 工程项目”的关键点。

## 项目一句话描述

> EchoMind 是一个基于 LLM 的企业级智能客服系统，集成三路融合意图识别、多 Agent 路由编排、动态 Skills 规则注入、Redis + ChromaDB 三级记忆、RAG 知识库增强回复、MCP 工具调用优化、Monitor 在线降权闭环和 LLM-as-Judge 端到端评测。

## 简历项目经历写法

### 通用版本

```text
EchoMind 智能客服系统
技术栈：Python / FastAPI / Anthropic Claude / DeepSeek Compatible API / Redis / ChromaDB / Docker / Prometheus

- 设计并实现三路融合意图识别模块，结合 LLM 语义理解、Embedding 相似度和关键词 Pattern，
  通过加权投票输出意图、置信度、紧急度和结构化实体；兼容第三方 API 时自动禁用 Embedding 分支。
- 实现 MCP 工具调用框架，封装参数校验、TTL 缓存、超时控制、熔断器和 fallback，
  并通过查询改写、并行召回和 LLM 重排优化 RAG 知识库检索质量。
- 将 RAG 知识库检索接入 `/chat` 主链路，在 Agent 回复前自动检索 ChromaDB `knowledge_base`
  并注入 Top-K 知识片段，降低模型幻觉并提升业务规则回答准确性。
- 实现动态 Skills 加载能力，支持从 Markdown/JSON 文件加载通用客服、技术支持、账单退款规范，
  按 Agent 类型和用户消息关键词注入 system prompt，并通过 `/skills/reload` 热更新。
- 设计 Redis + ChromaDB 三级记忆架构，使用 Redis 管理当前会话工作记忆，
  使用 ChromaDB 存储历史对话摘要和用户画像，支持自动压缩和跨会话语义检索。
- 实现多 Agent 编排系统，支持意图路由、性能路由和降级路由；
  对同时涉及技术和账单的复合问题自动并行派发给多个 Agent 并合并结果。
- 构建 Monitor 在线监控闭环，定时采集 Agent/工具成功率和延迟，
  通过 monitor_penalty 写回 Orchestrator，动态降低问题 Agent 的 routing_score。
- 实现端到端评测框架，真实调用 Orchestrator 生成回复，并使用 LLM-as-Judge
  从相关性、准确性、完整性和有用性四个维度评分，支持回归检测和优化建议生成。
```

### 后端/架构方向版本

```text
- 基于 FastAPI 构建智能客服 API，封装 /chat、/search、/knowledge、/monitor、/eval 等接口，
  使用 Docker Compose 编排 API、Redis、ChromaDB、Prometheus 和 Nginx。
- 设计工具调用可靠性框架，实现 JSON Schema 参数校验、TTL 缓存、工具级超时、熔断器和 fallback，
  避免外部依赖故障拖垮主链路。
- 设计多 Agent 路由层，通过 AgentStats 记录成功率和延迟，并结合 Monitor 写回的 monitor_penalty
  动态计算 routing_score，支持故障降级和问题 Agent 自动降权。
- 设计 Skills 规则加载层，将运营侧客服规范与代码解耦，支持运行时重新加载和按 Agent 隔离注入。
- 设计三级记忆架构，使用 Redis 存储 24h 工作记忆，使用 ChromaDB 存储 episodic 历史摘要和
  user_profile 用户画像，解决多轮对话 context 膨胀问题。
```

### AI Agent / NLP 方向版本

```text
- 实现端到端意图识别器，使用 LLM Few-shot、Embedding 相似度和关键词 Pattern 三路融合，
  通过动态权重和低置信度降级降低误判风险。
- 设计 RAG 检索优化链路，使用 LLM 将用户查询改写为多个角度的子查询，
  并行召回 ChromaDB 知识库后进行合并去重和 LLM 重排。
- 实现多 Agent 编排与协作，依据意图将请求路由到 General、Technical、Billing Agent，
  对技术 + 账单等复合问题自动并行处理。
- 实现动态 Skills 注入机制，让不同 Agent 使用各自的业务处理规范，降低越权回复和话术漂移风险。
- 实现 LLM-as-Judge 评测流程，覆盖意图识别准确率、端到端回复质量、回归检测和优化建议。
```

### 搜索/RAG 方向版本

```text
- 基于 ChromaDB 构建 RAG 知识库，支持默认文档导入、批量 JSON 导入、Markdown/TXT 上传和自动切片。
- 设计查询改写 + 并行召回 + 合并去重 + LLM 重排的检索优化链路，
  解决单查询召回不全和向量相似度排序不等于答案质量的问题。
- 将知识库 Top-K 检索结果注入 `/chat` 的 Agent 上下文，使客服回复优先依据企业知识库，
  文档更新后无需改代码即可影响主对话回答。
- 将知识库检索封装为 MCP 工具，统一接入缓存、熔断、参数校验、超时和 fallback，
  并通过 ToolStats 暴露工具成功率、延迟和熔断状态。
```

## 面试高频问题及回答思路

### Q1：端到端意图识别是怎么做的？为什么用三路融合？

**回答思路**

我没有只依赖单一策略，而是做了三路融合：

- LLM 负责复杂语义理解和上下文依赖。
- Embedding 负责常见表达和模板的相似度匹配。
- Pattern 负责零延迟兜底，覆盖退款、报错、转人工等高频关键词。

官方 API 模式下权重是 LLM 70%、Embedding 20%、Pattern 10%；如果配置了第三方兼容 API，比如 DeepSeek，因为很多兼容 API 不支持 Embedding，系统会自动切换为 LLM 85% + Pattern 15%。投票得分低于阈值时会降级为 `OTHER`，减少错误路由。

**可以补充**

当前 Anthropic SDK 没有 `embeddings` 资源，所以我做了远端优先、本地字符 n-gram 哈希向量兜底。这样官方模式下 Embedding 分支仍然可运行，不会因为 SDK 缺能力导致三路融合失效。

### Q2：检索召回不好怎么优化？

**回答思路**

我把知识库检索封装成 MCP 工具，并设计了完整检索优化链路：

```text
原始查询 -> LLM 查询改写 -> 多子查询并行召回 -> 合并去重 -> LLM 重排 -> Top-K
```

查询改写解决召回不全。例如“退款流程”会被扩展成“如何申请退款”“退款需要多久”“退款政策是什么”等多个角度。并行召回控制延迟，合并去重减少重复片段。最后用 LLM 重排，因为向量相似度高不一定代表对用户最有帮助。

**可以补充**

工具框架还有 TTL 缓存、JSON Schema 参数校验、超时控制、熔断器和 fallback。知识库不可用时会返回可解释降级结果，而不是直接抛空错误。

### Q2.1：为什么要把知识库检索接进 `/chat`？

**回答思路**

如果知识库只通过 `/search` 暴露，它只是旁路演示功能；用户正常聊天时 Agent 仍然主要依赖模型常识和对话记忆。接入 `/chat` 后，主链路会自动检索 ChromaDB `knowledge_base`，把 Top-K 结果作为上下文给 Agent。

这样做有三个业务价值：

- 降低幻觉：退款政策、配送规则、故障排查步骤以知识库为准。
- 知识可维护：业务规则变化时只需要更新知识库，不需要改 prompt 或代码。
- 主链路闭环：RAG 不再只是搜索接口，而是真正参与客服回复。

### Q3：多轮对话记忆怎么设计？

**回答思路**

我用了三级记忆：

| 层级 | 存储 | 作用 |
|------|------|------|
| 工作记忆 | Redis | 当前会话最近消息，24h TTL |
| 情景记忆 | ChromaDB `episodic` | 压缩后的历史对话摘要，可语义检索 |
| 用户画像 | ChromaDB `user_profile` | 从对话中提炼用户偏好和关键实体 |

当前会话消息先写入 Redis。达到压缩阈值后，用 LLM 总结旧消息，把摘要写入 Redis summary 和 ChromaDB `episodic`，工作记忆只保留最近 5 条。这样既保留当前上下文，又避免 prompt 无限增长。

**可以补充**

每次 `/chat` 回复后会异步更新用户画像，不阻塞主请求。下一次同一用户请求时，画像会作为 `[用户画像]` 拼入 Agent 背景信息，用于个性化回复。

### Q4：多 Agent 路由怎么做？

**回答思路**

路由分三层：

1. 意图映射：技术问题到 `TechnicalAgent`，账单/账户问题到 `BillingAgent`，其他到 `GeneralAgent`。
2. 性能路由：同类 Agent 多实例时，按 `routing_score` 选择表现最好的实例。
3. 降级路由：专属 Agent 不可用或失败时，降级到 `GeneralAgent`。

`routing_score` 不只是成功率，还考虑平均延迟和 Monitor 写回的 `monitor_penalty`：

```text
base_score = success_rate * 0.7 + latency_score * 0.3
routing_score = base_score * (1 - monitor_penalty)
```

**可以补充**

当前主链路支持自动并行协作。如果用户说“登录报错 401，而且还重复扣款”，系统会识别到技术和账单两个领域，自动并行调用 `TechnicalAgent` 和 `BillingAgent`，再合并结果。

### Q4.1：Skills 和知识库有什么区别？为什么要单独做？

**回答思路**

知识库解决的是“业务事实是什么”，例如退款政策、配送规则、会员积分说明；Skills 解决的是“客服应该怎么处理”，例如要先收集哪些信息、什么情况转人工、哪些敏感信息不能索取、账单退款不能做什么承诺。

我把 Skills 做成 `core/skill_loader.py`，启动时从 `ECHOMIND_SKILLS_DIR` 扫描 `SKILL.md`、Markdown、TXT 或 JSON 文件，解析 `keywords`、`agents`、`enabled` 等字段。Agent 调 LLM 前会根据当前消息和 Agent 类型筛选规则，并拼入 system prompt。

**可以补充**

目前内置三类 Skills：通用客服、技术支持、账单退款。技术问题只注入技术支持规范，账单问题只注入账单退款规范，避免不同领域规则互相污染。修改规则后可以通过 `POST /skills/reload` 热加载，不需要重启服务。

### Q5：Monitor 怎么形成闭环？

**回答思路**

Monitor 每 10 秒采集 Orchestrator 和 ToolManager 的统计数据，包括 Agent 成功率、平均延迟、工具成功率、工具延迟和连续失败次数。

闭环体现在它不只是展示指标，还会计算每个 Agent 的 `monitor_penalty`，然后调用：

```python
Orchestrator.update_routing_penalties()
```

把惩罚项写回 AgentStats。下一次 `_best_agent()` 选择 Agent 时，`routing_score` 会受这个惩罚项影响，表现差的 Agent 会自动降权。

### Q6：端到端 Agent 怎么评测？

**回答思路**

评测分两部分：

1. 意图识别评测：用标注用例计算 Accuracy 和 Macro-F1。
2. 端到端回复评测：真实调用 `Orchestrator.run()` 生成回复，再用 LLM-as-Judge 从四个维度打分。

四个维度是：

- `relevance`：是否回答用户问题。
- `accuracy`：信息是否准确。
- `completeness`：是否完整解决需求。
- `helpfulness`：用户能否据此行动。

评测器还会和上一轮结果对比，如果某个指标退化超过 5%，会标记为 regression，并生成优化建议。

### Q7：ChromaDB 在项目里怎么用？

**回答思路**

ChromaDB 有三个 collection：

| Collection | 用途 |
|------------|------|
| `knowledge_base` | RAG 知识库，存业务文档片段 |
| `episodic` | 情景记忆，存压缩后的历史对话摘要 |
| `user_profile` | 用户画像，存偏好和关键实体 |

这三个 collection 分工不同，避免知识库文档和用户记忆混在一起。Docker Compose 中 ChromaDB 单独运行，应用通过 `chromadb.HttpClient(host="chromadb", port=8000)` 连接。

## 技术栈关键词

```text
LLM / Anthropic Claude / DeepSeek Compatible API / Agent Orchestration /
Intent Recognition / Few-shot Prompting / Embedding Fallback /
Dynamic Skills / Prompt Injection Guardrails /
RAG / Query Rewriting / Reranking / MCP Tool Calling /
Redis / ChromaDB / Vector Search / Conversation Memory /
FastAPI / Docker Compose / Prometheus /
LLM-as-Judge / Evaluation / Circuit Breaker / TTL Cache / Fallback
```

## 量化指标写法建议

如果没有真实压测和标注集，不建议在简历中写绝对数值。可以使用下面的安全写法。

| 指标类型 | 推荐写法 | 避免写法 |
|----------|----------|----------|
| 意图识别 | “在内置评测集上统计 Accuracy / Macro-F1” | “准确率 92%+” |
| 延迟 | “通过并行调用减少串行等待” | “P95 < 800ms” |
| 检索 | “通过查询改写扩大召回覆盖面” | “召回率提升 40%” |
| 稳定性 | “工具级熔断、超时、fallback 降级” | “99.99% 可用性” |
| 部署 | “Docker Compose 一键启动完整栈” | “5 分钟部署所有环境” |

如果已经做过实际测试，可以改成：

```text
- 在 8 类内置意图测试集上评估 IntentRecognizer，输出 Accuracy 和 Macro-F1；
  评测结果通过 /eval/run 自动生成，并支持与上一轮结果做回归对比。
```

## 简历短版 Bullet

适合放在简历里，保留 4-6 条即可。

```text
- 设计 LLM + Embedding + Pattern 三路融合意图识别，支持动态权重、低置信度降级和实体抽取。
- 基于 ChromaDB 构建 RAG 知识库，实现查询改写、并行召回、合并去重和 LLM 重排。
- 实现动态 Skills 加载与热更新，按 Agent 类型注入通用客服、技术支持和账单退款规范。
- 设计 Redis + ChromaDB 三级记忆，支持工作记忆 TTL、历史摘要压缩和用户画像提炼。
- 实现多 Agent 路由编排，支持意图路由、性能路由、失败降级和复合问题自动并行协作。
- 构建 Monitor 闭环，根据成功率和延迟计算 monitor_penalty，动态调整 Agent routing_score。
- 实现 LLM-as-Judge 端到端评测，覆盖回复质量评分、回归检测和优化建议生成。
```

## 30 秒项目 Pitch

> EchoMind 是一个基于 LLM 的智能客服系统。我主要解决了七个工程问题：第一，意图识别用 LLM、Embedding 和 Pattern 三路融合；第二，知识库检索用查询改写、并行召回和 LLM 重排，并接入 `/chat` 主链路增强 Agent 回复；第三，通过动态 Skills 把通用客服、技术支持和账单退款规范按 Agent 类型注入 prompt；第四，多轮对话用 Redis 工作记忆、ChromaDB 情景记忆和用户画像管理上下文；第五，多 Agent 通过意图路由、性能路由和降级路由协作处理问题；第六，Monitor 会根据线上成功率和延迟写回降权因子，动态影响后续路由；第七，用 LLM-as-Judge 做端到端评测和回归检测。整个系统可以用 Docker Compose 一键启动。

## 面试时的注意事项

- 不要把“设计目标”说成“线上真实指标”，除非你确实跑过压测或标注评测。
- 讲 Monitor 时要强调当前实现是 `monitor_penalty` 写回 Orchestrator，不只是展示 dashboard。
- 讲并行协作时要强调当前主链路会自动检测复合问题，不需要外部手动调用 `run_parallel()`。
- 讲 Skills 时要强调它和知识库的边界：知识库管事实，Skills 管处理规范和安全边界。
- 讲 ChromaDB 时要明确三个 collection 的边界：`knowledge_base`、`episodic`、`user_profile`。
- 讲 Embedding 时要说明第三方兼容 API 默认禁用，官方模式下支持远端优先、本地哈希向量兜底。
